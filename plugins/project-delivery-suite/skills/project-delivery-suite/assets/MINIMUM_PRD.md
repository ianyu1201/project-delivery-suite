# <PROJECT> <VERSION> 产品需求文档

> 本模板仅在项目没有现役 PRD 规范、且没有可用的专业 PRD Skill 时作为最小回退。复用项目已有文件名、语言和结构；同一版本只保留一份现役 PRD。

## 文档状态

| 字段 | 内容 |
|---|---|
| 版本 | `<project-version>` |
| 状态 | `draft / approved / superseded` |
| 产品负责人 | `<authority owner>` |
| 批准人 | `<approver / pending>` |
| 批准时间 | `<date / pending>` |
| 批准证据 | `<decision link, message, commit, or pending>` |
| 来源基线 | `<project brief, prior approved PRD, decisions, research>` |
| 替代文档 | `<superseded PRD or none>` |

没有精确内容的授权证据时，状态必须保持 `draft`。批准 PRD 只批准产品范围，不代表代码完成、验收通过、版本批准或已经发布。

## 1. 产品目标

- 目标用户：
- 用户问题：
- 目标结果：
- 成功信号：
- 本版本为什么现在要做：

## 2. 用户与核心场景

| 场景 ID | 用户/角色 | 触发条件 | 用户目标 | 可观察结果 |
|---|---|---|---|---|
| SCN-001 |  |  |  |  |

## 3. 本版本范围

### 必须包含

-

### 明确不包含

-

### 延后处理

-

## 4. 产品需求

| 需求 ID | 需求行为（what） | 用户价值（why） | 优先级 | 状态 | 来源/决定 |
|---|---|---|---|---|---|
| REQ-001 |  |  | Must | proposed |  |

要求：

- 每项需求使用稳定 ID；修订需求时保留 ID 和来源。
- 描述产品行为和用户结果，不在 PRD 中塞入框架、类名或像素级实现方案。
- 删除、隐私、安全、兼容性、迁移和不可逆行为必须写明，不得从版本沉默推断为已删除。

## 5. 状态、异常与边界

| ID | 正常/空/加载/错误/权限/边界状态 | 预期产品行为 | 用户可见反馈 |
|---|---|---|---|
| STATE-001 |  |  |  |

## 6. 数据与约束

- 用户输入和输出：
- 数据保留、删除与恢复：
- 隐私、安全与权限：
- 法律、平台或业务约束：
- 用户可感知的性能与可用性要求：

不适用的项目明确写 `N/A` 及理由，不要静默省略高影响约束。

## 7. 验收条件

| 验收 ID | 关联需求/场景 | 前置条件 | 操作 | 可观察预期结果 |
|---|---|---|---|---|
| AC-001 | REQ-001 |  |  |  |

验收条件描述用户或验收者可观察的结果。测试层级、代码结构和证据采集方式进入后续交付合同。

## 8. 开放问题与冲突

| 决策 ID | 问题/冲突 | 可选方案 | 推荐方案及影响 | 授权人 | 状态/证据 |
|---|---|---|---|---|---|
| DEC-001 |  |  |  |  | open |

影响范围、数据、安全、兼容性或验收的重要问题未解决时，PRD 不得批准。允许隔离的问题必须注明不进入本版本的处理方式。

## 9. 追溯与变更

| 日期 | 变更 | 影响的需求/场景 | 决定人 | 证据 |
|---|---|---|---|---|
| YYYY-MM-DD | 初始草稿 |  |  |  |
